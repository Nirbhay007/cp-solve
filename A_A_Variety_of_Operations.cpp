#include <bits/stdc++.h>

using namespace std;

typedef long long ll;
int main()
{

	int t;
	cin >> t;

	while (t--)
	{
		ll a = 0, b = 0;
		ll c, d;
		cin >> c >> d;
		int count = 0;
		for (int i = 1; i <= 1e9; i++)
		{

		if(a+i)
		}
	}
}